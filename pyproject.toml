[project]
name = "vcdview"
version = "0.1.0"
description = "Console VCD waveform viewer"
requires-python = ">=3.10"
dependencies = [
    "prompt_toolkit>=3.0",
    "colorama>=0.4.6"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

